<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>My Space</title>
    <link href="https://fonts.googleapis.com/css2?family=Poppins:wght@400;500;600&display=swap" rel="stylesheet">
    <style>
        body { 
            font-family: 'Poppins', sans-serif; 
            /* Beautiful "Dreamy" Gradient */
            background-image: linear-gradient(to top, #a18cd1 0%, #fbc2eb 100%);
            display: flex; 
            justify-content: center; 
            align-items: center; 
            min-height: 100vh; 
            margin: 0; 
            color: #4a4a4a;
        }

        .container { 
            background: rgba(255, 255, 255, 0.9); 
            padding: 3rem; 
            border-radius: 24px; 
            box-shadow: 0 15px 35px rgba(0,0,0,0.1); 
            width: 420px; 
            backdrop-filter: blur(10px); /* Frosted glass effect */
        }

        h2 { 
            text-align: left; 
            color: #5f2c82; /* Deep purple text */
            font-size: 28px; 
            margin-bottom: 5px; 
            font-weight: 600;
        }

        p.subtitle {
            color: #999;
            font-size: 14px;
            margin-top: 0;
            margin-bottom: 25px;
        }

        form { 
            display: flex; 
            gap: 12px; 
            margin-bottom: 30px; 
        }

        input { 
            flex: 1; 
            padding: 15px 20px; 
            border: 2px solid #f0f0f0; 
            background: #fff; 
            border-radius: 14px; 
            font-family: 'Poppins', sans-serif;
            font-size: 14px;
            outline: none;
            color: #555;
            transition: 0.3s;
        }
        
        input:focus {
            border-color: #a18cd1;
        }

        button.add-btn { 
            background: #a18cd1; /* Matches background */
            color: white; 
            border: none; 
            padding: 15px 25px; 
            border-radius: 14px; 
            font-weight: 600; 
            cursor: pointer; 
            transition: 0.3s;
            box-shadow: 0 5px 15px rgba(161, 140, 209, 0.4);
        }
        
        button.add-btn:hover {
            transform: translateY(-2px);
            background: #8e74c0;
        }

        ul { 
            list-style: none; 
            padding: 0; 
        }

        li { 
            background: #fff; 
            margin-bottom: 12px; 
            padding: 15px;
            border-radius: 14px;
            display: flex; 
            justify-content: space-between; 
            align-items: center; 
            box-shadow: 0 4px 10px rgba(0,0,0,0.03);
            border-left: 5px solid #fbc2eb; /* soft pink accent */
            transition: 0.2s;
        }
        
        li:hover {
            transform: translateY(-2px);
        }

        .task-text {
            font-size: 15px;
            color: #555;
            font-weight: 500;
        }

        .delete-btn { 
            background: #fff5f5; 
            color: #ff9f9f; 
            border: none; 
            padding: 8px 12px; 
            border-radius: 8px;
            font-size: 12px; 
            font-weight: bold; 
            cursor: pointer; 
            transition: 0.2s;
        }

        .delete-btn:hover {
            background: #ff7675;
            color: white;
        }
    </style>
</head>
<body>
    <div class="container">
        <h2>Your Space</h2>
        <p class="subtitle">What are we doing today?</p>
        
        <form action="/add" method="POST">
            <input type="text" name="taskName" placeholder="Add a new goal..." required>
            <button type="submit" class="add-btn">Add</button>
        </form>
        
        <ul>
            <% tasks.forEach(task => { %>
                <li>
                    <span class="task-text"><%= task.name %></span>
                    <form action="/delete" method="POST" style="display:inline;">
                        <input type="hidden" name="taskId" value="<%= task._id %>">
                        <button type="submit" class="delete-btn">Delete</button>
                    </form>
                </li>
            <% }) %>
        </ul>
    </div>
</body>
</html>