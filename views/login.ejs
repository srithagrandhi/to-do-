<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Login</title>
    <link href="https://fonts.googleapis.com/css2?family=Poppins:wght@400;600&display=swap" rel="stylesheet">
    <style>
        body { 
            font-family: 'Poppins', sans-serif; 
            background-image: linear-gradient(to top, #a18cd1 0%, #fbc2eb 100%);
            display: flex; 
            justify-content: center; 
            align-items: center; 
            min-height: 100vh; 
            margin: 0; 
        }

        .container { 
            background: rgba(255, 255, 255, 0.95); 
            padding: 3rem; 
            border-radius: 24px; 
            box-shadow: 0 15px 35px rgba(0,0,0,0.1); 
            width: 350px; 
            text-align: center;
        }

        h2 { 
            color: #5f2c82; 
            margin-bottom: 5px; 
            font-size: 26px;
            font-weight: 600;
        }

        p.subtitle { color: #999; font-size: 14px; margin-bottom: 30px; margin-top: 0; }

        input { 
            width: 100%; 
            padding: 15px; 
            margin-bottom: 15px; 
            border: 2px solid #f0f0f0; 
            border-radius: 14px; 
            font-family: 'Poppins', sans-serif;
            font-size: 14px;
            outline: none;
            box-sizing: border-box;
            transition: 0.3s;
        }

        input:focus { border-color: #a18cd1; background: #fff; }

        button { 
            width: 100%; 
            background: #a18cd1; 
            color: white; 
            border: none; 
            padding: 15px; 
            border-radius: 14px; 
            font-weight: 600; 
            cursor: pointer; 
            transition: 0.3s;
            font-size: 16px;
            box-shadow: 0 5px 15px rgba(161, 140, 209, 0.4);
        }

        button:hover { background: #8e74c0; transform: translateY(-2px); }

        .link { margin-top: 25px; display: block; font-size: 13px; color: #a18cd1; text-decoration: none; font-weight: 500; }
        .link:hover { color: #5f2c82; }
    </style>
</head>
<body>
    <div class="container">
        <h2>Welcome Back</h2>
        <p class="subtitle">Please enter your details.</p>
        <form action="/login" method="POST">
            <input type="text" name="username" placeholder="Username" required>
            <input type="password" name="password" placeholder="Password" required>
            <button type="submit">Sign In</button>
        </form>
        <a href="/register" class="link">Create an account</a>
    </div>
</body>
</html>